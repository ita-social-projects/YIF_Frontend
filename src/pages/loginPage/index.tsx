import React from 'react';
import { Header, LoginForm } from '../../components';

const LoginPage = () => {
  return (
    <>
      <Header />
      <LoginForm />
    </>
  );
};

export default LoginPage;

import React from 'react';

const AddSpecialties = (props: any) => {
  return (
    <>
      <h2>Add Specialties Component</h2>;
      <button onClick={props.onChangeBlock}></button>
    </>
  );
};

export default AddSpecialties;
